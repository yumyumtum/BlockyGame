# 游戏代码逻辑说明文档

本文档详细解释 BlockyGame 游戏集合中每个游戏的代码逻辑和架构。

## 目录
- [SpaceShip (星际飞船)](#spaceship-星际飞船)
- [Dodge_Games (躲避游戏)](#dodge_games-躲避游戏)
- [FlappyPoop (飞扬小鸟)](#flappypoop-飞扬小鸟)
- [SniperGame (狙击手游戏)](#snipergame-狙击手游戏)
- [SigmaSniper (西格玛狙击手)](#sigmasniper-西格玛狙击手)
- [DuckAdventure (鸭子冒险)](#duckadventure-鸭子冒险)
- [Trivia (问答游戏)](#trivia-问答游戏)

---

## SpaceShip (星际飞船)

### 概述
一款太空射击游戏,玩家控制飞船与敌人战斗并收集能量道具。

### 核心组件

#### 1. 游戏状态管理
```javascript
let spaceship = { x, y, width, height, speed }  // 玩家飞船
let bullets = []      // 玩家子弹
let missiles = []     // 火箭弹药
let enemies = []      // 敌人飞船
let enemyBullets = [] // 敌人子弹
let stars = []        // 能量道具(火箭拾取物)
let assistShips = []  // 辅助飞船拾取物
let shadowShips = []  // 激活的影子飞船
let hexItems = []     // 金色星星拾取物
```

#### 2. 玩家控制
- **WASD / 方向键**: 四向移动
- **空格键**: 发射子弹(长按为激光束)
- **'2' / 右键**: 发射火箭
- **左键点击**: 发射子弹

#### 3. 武器系统

**普通子弹:**
- 快速向上移动的弹丸
- 无限弹药
- 基础伤害

**激光束:**
- 长按空格键发射持续激光
- 高伤害输出
- 20秒冷却时间
- 激活时消耗能量

**火箭:**
- 有限弹药(从星星拾取物获得)
- 爆炸范围伤害
- 可以同时消灭多个敌人

#### 4. 敌人类型

**射击型敌人:**
- 垂直移动并发射弹丸
- 标准生命值
- 可预测的移动模式

**闪烁型敌人:**
- 周期性出现/消失
- 被击杀时掉落能量道具
- 死亡时产生大爆炸(可伤害玩家)

**旋转型敌人:**
- 旋转移动
- 被摧毁时分裂成2个小敌人
- 中等爆炸效果

**迷你敌人:**
- 小型、快速移动
- 由分裂敌人生成
- 直接追击玩家

#### 5. 能量道具系统

**星星拾取物(黄色):**
- 获得火箭弹药(+1)
- 敌人常见掉落物

**辅助飞船(绿色):**
- 生成辅助飞船并肩作战
- 自动瞄准并射击敌人
- 持续时间有限

**影子飞船(紫色/六边形):**
- 临时无敌
- 镜像玩家移动
- 视觉影子效果

**金色星星(稀有):**
- 特殊收集品
- 额外得分

#### 6. 难度递增
- **等级系统**: 每60秒提升一级
- **速度倍增器**: 每级敌人速度提升20%(×1.2)
- **5级重置**: 到达5级时速度重置,然后再次递增
- 应用于:
  - 敌人移动速度
  - 敌人子弹速度
  - 生成速率调整

#### 7. 碰撞检测
```javascript
function isColliding(a, b) {
  return !(a.x > b.x + b.width || 
           a.x + a.width < b.x || 
           a.y > b.y + b.height || 
           a.y + a.height < b.y)
}
```
- 基于矩形的碰撞检测
- 每帧检测:
  - 玩家 vs 敌人
  - 玩家 vs 敌人子弹
  - 玩家子弹 vs 敌人
  - 玩家 vs 能量道具

#### 8. 爆炸系统
```javascript
createExplosion(x, y, size, color, canKill)
```
- 基于粒子的爆炸效果
- 扩展半径动画
- 根据敌人类型不同尺寸:
  - 闪烁型敌人: 240px半径(可杀死玩家)
  - 旋转型敌人: 50px半径
  - 迷你敌人: 25px半径
  - 普通敌人: 40px半径
- 粒子随时间淡出

#### 9. 视觉效果

**背景星空:**
- 150颗星星视差滚动
- 闪烁动画(正弦波)
- 多层深度

**飞船动画:**
- 三个精灵图像: 正常、左倾、右倾
- 根据移动方向改变
- 平滑过渡

**屏幕震动:**
- 爆炸时触发
- 强度基于爆炸大小

#### 10. 音频系统
- **射击音效**: 子弹发射
- **火箭音效**: 火箭发射
- **拾取音效**: 收集物品
- **爆炸音效**: 敌人毁灭
- **激光音效**: 光束武器
- **背景音乐**: 3首曲目循环(background.mp3, DestroyThemAll.mp3, Starfield.mp3)

#### 11. 游戏循环
```javascript
function gameLoop() {
  if (!menuActive && !gameOver && !paused) {
    update()    // 更新位置,检查碰撞
    draw()      // 渲染所有对象
    requestAnimationFrame(gameLoop)
  }
}
```

**更新阶段:**
1. 根据按键输入移动玩家
2. 更新所有子弹/火箭位置
3. 更新敌人位置和AI
4. 更新敌人子弹
5. 处理碰撞
6. 更新能量道具
7. 更新爆炸效果
8. 检查胜负条件

**绘制阶段:**
1. 清除画布
2. 绘制背景星星
3. 绘制敌人
4. 绘制玩家
5. 绘制弹丸
6. 绘制能量道具
7. 绘制爆炸效果
8. 绘制UI(生命值、分数等)

#### 12. 计分系统
- 击杀数追踪消灭的敌人
- 每10次击杀获得1枚火箭
- 实时显示分数
- 排名保存到localStorage

---

## Dodge_Games (躲避游戏)

### 概述
一款生存游戏,玩家控制黄色笑脸躲避下落的方块并收集能量道具升级。

### 核心组件

#### 1. 游戏状态
```javascript
let player = { x, y, width, height, speed, health, level }
let blocks = []       // 下落的障碍物
let arrows = []       // 玩家弹丸(5级以上)
let missiles = []     // 玩家导弹(10级以上)
let explosions = []   // 视觉效果
let score = 0         // 成功躲避的方块数
```

#### 2. 玩家控制
- **方向键 / WASD**: 四向移动
- **5**: 发射箭头(需要等级 ≥ 5)
- **1**: 发射导弹(需要等级 ≥ 10)
- **L / 鼠标中键**: 按住发射激光束
- **H**: 切换帮助界面

#### 3. 方块类型

**红色方块:**
- 接触时伤害玩家(-1 HP, 30秒后-1.5 HP)
- 最常见的方块类型(60%)
- 必须躲避

**蓝色方块:**
- 治疗玩家(+1 HP,最大2)
- 增加玩家等级(+1)
- 应该收集(20%)

**绿色方块:**
- 接触立即游戏结束
- 60秒后变成橙色
- 必须避开(10%)

**黄色方块(增益):**
- 再生增益(15秒)
- 每秒恢复0.5 HP
- 稀有(4%)

**紫色方块(增益):**
- 速度提升(10秒)
- 移动速度翻倍
- 稀有(3%)

**白色方块(增益):**
- 无敌(8秒)
- 免疫伤害
- 非常稀有(2%)

**粉色方块(增益):**
- 护盾(12秒)
- 吸收一次伤害
- 非常稀有(1%)

#### 4. 弹丸系统

**箭头(5级以上):**
- 快速、简单的弹丸
- 接触时摧毁方块
- 按'5'键发射

**导弹(10级以上):**
- 较慢但更强大
- 撞击时产生爆炸
- 击中增益方块时可激活增益
- 按'1'键发射

**激光(任何等级):**
- 按住'L'或鼠标中键
- 持续光束摧毁方块
- 无冷却时间

#### 5. 增益系统

**再生(黄色):**
```javascript
regenEnd = now + 15000
healTick = 每1000ms
health += 0.5 (最大2)
```

**速度提升(紫色):**
```javascript
speedEnd = now + 10000
player.speed = baseSpeed * 2
```

**无敌(白色):**
```javascript
invEnd = now + 8000
// 忽略所有伤害
```

**护盾(粉色):**
```javascript
shieldEnd = now + 12000
shieldActive = true
// 吸收下一次伤害,然后停用
```

#### 6. 碰撞逻辑
```javascript
function checkCollision(a, b) {
  return a.x < b.x + b.width && 
         a.x + a.width > b.x &&
         a.y < b.y + b.height && 
         a.y + a.height > b.y
}
```

**优先级顺序:**
1. 检查无敌(忽略伤害)
2. 检查护盾(吸收伤害)
3. 根据方块类型应用伤害/效果

#### 7. 计分系统
- **分数**: 未碰撞掉落到屏幕外的方块数量
- **等级**: 通过收集蓝色方块提升
- **生命值**: 最大2.0,显示为条形图

#### 8. 难度递进
- 方块生成速率: 每800ms
- 方块速度: 每帧2-4像素(随机)
- 速度随时间略微增加
- 绿色方块60秒后变成橙色(仍然致命)

#### 9. 视觉反馈

**玩家:**
- 带黑色眼睛和笑脸的黄色圆圈
- 根据状态改变表情
- 受到伤害时闪烁红色

**警告系统:**
```javascript
warningTimer = 60  // 帧
// 受击时屏幕短暂闪红
```

**爆炸:**
- 扩展的圆圈与粒子效果
- 绿色方块产生更大的爆炸
- 随时间淡出

#### 10. 音乐系统
- 5首背景音乐随机循环
- 文件: BANDIT.mp3, ICE_AGE.mp3, TORE_UP.mp3, Toxic.mp3, Understand.mp3
- 音量设置为0.5
- 持续循环播放

#### 11. 游戏结束条件
1. 玩家生命值降至0
2. 玩家接触绿色方块
3. 显示最终分数和等级

#### 12. 游戏循环
```javascript
function gameLoop() {
  if (gameRunning) {
    updateBlocks()
    updateProjectiles()
    updateExplosions()
    updateBuffs()
    movePlayer()
    draw()
    requestAnimationFrame(gameLoop)
  }
}
```

---

## FlappyPoop (飞扬小鸟)

### 概述
简化版Flappy Bird克隆,黄色小鸟穿越绿色管道。

### 核心组件

#### 1. 游戏状态
```javascript
let bird = { x: 80, y: 200, width: 40, height: 30, velocity: 0 }
let pipes = []
let score = 0
let gameOver = false
```

#### 2. 物理常量
```javascript
const GRAVITY = 0.5         // 向下加速度
const FLAP = -8            // 拍翅膀时的向上速度
const PIPE_WIDTH = 60      // 障碍物宽度
const PIPE_GAP = 150       // 管道之间的垂直间隙
```

#### 3. 控制
- **空格键**: 拍翅膀(跳跃)
  - 游戏中: 施加向上速度
  - 游戏结束时: 重新开始游戏

#### 4. 小鸟物理
```javascript
function update() {
  bird.velocity += GRAVITY    // 应用重力
  bird.y += bird.velocity     // 更新位置
}

function flap() {
  bird.velocity = FLAP        // 设置向上速度
}
```

#### 5. 管道生成
```javascript
function createPipe() {
  const topHeight = Math.random() * (canvas.height - PIPE_GAP - 100) + 20
  pipes.push({
    x: canvas.width,
    top: topHeight,
    bottom: canvas.height - topHeight - PIPE_GAP
  })
}
```
- 每1500ms生成一次
- 随机垂直位置
- 以恒定速度向左移动(每帧2像素)

#### 6. 碰撞检测

**边界检查:**
```javascript
if (bird.y + bird.height > canvas.height || bird.y < 0) {
  gameOver = true
}
```

**管道碰撞:**
```javascript
if (bird.x < pipe.x + PIPE_WIDTH &&
    bird.x + bird.width > pipe.x &&
    (bird.y < pipe.top || 
     bird.y + bird.height > canvas.height - pipe.bottom)) {
  gameOver = true
}
```

#### 7. 计分系统
```javascript
if (!pipe.passed && pipe.x + PIPE_WIDTH < bird.x) {
  score++
  pipe.passed = true  // 防止重复计数
}
```
- 每通过一个管道得1分
- 每个管道只计数一次

#### 8. 渲染
- **背景**: 天蓝色(#70c5ce)
- **小鸟**: 黄色矩形(40×30)
- **管道**: 绿色矩形(60px宽)
- **分数显示**: 顶部中央的白色文字

#### 9. 游戏循环
```javascript
function gameLoop() {
  if (!gameOver) {
    update()      // 物理和碰撞
    draw()        // 渲染
    requestAnimationFrame(gameLoop)
  } else {
    // 显示游戏结束消息
  }
}
```

#### 10. 游戏重置
```javascript
function resetGame() {
  bird.y = 200
  bird.velocity = 0
  pipes = []
  score = 0
  gameOver = false
}
```

---

## SniperGame (狙击手游戏)

### 概述
狙击射击游戏,玩家通过瞄准镜瞄准并在移动的目标射击你之前击中它。

### 核心组件

#### 1. 游戏状态
```javascript
let target = {
  x, y,                    // 位置
  width: 30, height: 90,   // 大小
  speed: 5,                // 移动速度
  alive: true,             // 状态
  nextShotTime: Date.now() + 4000  // 目标何时射击
}
let bullet = null          // 玩家的射击
let bloodSplat = []       // 目标上的血迹颗粒
let screenBlood = []      // 屏幕上的血迹(玩家被击中时)
let gameOver = false
let message = ""
```

#### 2. 控制
- **鼠标位置**: 瞄准
- **左键点击**: 射击

#### 3. 视觉元素

**瞄准镜:**
```javascript
function drawScope() {
  // 圆圈: 200px半径
  // 十字准星: 水平和垂直线
}
```
- 白色圆圈(200px半径)
- 屏幕中心的十字准星
- 始终居中于屏幕

**目标:**
```javascript
function drawRealisticTarget(x, y) {
  // 身体: 绿色矩形(#556B2F)
  // 头部: 米色圆圈(#FFDAB9)
  // 手臂: 从肩膀延伸的米色线条
  // 腿部: 深色线条(#2F4F4F)
}
```
- 火柴人风格
- 90像素高
- 在屏幕上水平移动

#### 4. 游戏机制

**目标移动:**
- 从左到右或从右到左移动
- 速度: 每帧5像素
- 在屏幕边缘反向
- 随机垂直位置变化

**射击:**
```javascript
canvas.addEventListener("click", (e) => {
  if (!bullet && target.alive && !gameOver) {
    bullet = { 
      x: e.clientX, 
      y: e.clientY, 
      speed: 15 
    }
  }
})
```

**命中检测:**
```javascript
if (bullet.x >= target.x - 15 && 
    bullet.x <= target.x + 15 &&
    bullet.y >= target.y - 45 && 
    bullet.y <= target.y + 45) {
  // 命中!
}
```

#### 5. 血液效果

**血液飞溅(在目标上):**
```javascript
for (let i = 0; i < 50; i++) {
  bloodSplat.push({
    x: hitX + random(-20, 20),
    y: hitY + random(-20, 20),
    size: random(3, 10)
  })
}
```

**屏幕血迹(玩家被击中时):**
```javascript
for (let i = 0; i < 100; i++) {
  screenBlood.push({
    x: random(0, canvas.width),
    y: random(0, canvas.height),
    size: random(5, 20),
    opacity: random(0.3, 0.7)
  })
}
// 加上全屏红色覆盖
```

#### 6. 目标AI
```javascript
if (Date.now() >= target.nextShotTime && target.alive) {
  gameOver = true
  message = "You got shot!"
  // 应用屏幕血液效果
}
```
- 如果仍然存活,目标会在4秒后射击
- 玩家必须在计时器到期前击杀目标

#### 7. 胜负条件

**获胜:**
- 在目标射击你之前击中目标
- 消息: "You hit the target!"

**失败:**
- 目标先射击你
- 消息: "You got shot!"
- 屏幕被血覆盖

#### 8. 游戏循环
```javascript
function gameLoop() {
  ctx.clearRect(0, 0, canvas.width, canvas.height)
  
  if (target.alive && !gameOver) {
    updateTarget()
  }
  
  drawTarget()
  drawBullet()
  drawBlood()
  drawScreenBlood()
  drawScope()
  drawMessage()
  
  if (!gameOver) {
    requestAnimationFrame(gameLoop)
  }
}
```

---

## SigmaSniper (西格玛狙击手)

### 概述
第一人称狙击游戏,具有移动、跳跃和高级瞄准镜机制。

### 核心组件

#### 1. 游戏状态
```javascript
let player = {
  x, y, z,           // 3D空间中的位置
  vx, vy, vz,        // 速度
  hp: 100,           // 生命值
  onGround: true     // 跳跃状态
}
let enemies = []     // 敌人位置
let bullets = []     // 飞行中的子弹
let score = 0
```

#### 2. 控制
- **点击**: 锁定鼠标指针
- **ESC**: 解锁指针
- **鼠标移动**: 环顾四周
- **WASD**: 移动(前进、左、后退、右)
- **空格**: 跳跃
- **左键点击**: 射击
- **按住右键**: 瞄准镜(放大视野)

#### 3. 相机系统

**指针锁定API:**
```javascript
canvas.addEventListener('click', () => {
  canvas.requestPointerLock()
})

document.addEventListener('pointerlockchange', () => {
  // 更新锁定状态
})
```

**鼠标视角:**
```javascript
document.addEventListener('mousemove', (e) => {
  if (locked) {
    yaw += e.movementX * sensitivity
    pitch += e.movementY * sensitivity
    pitch = clamp(pitch, -89, 89)  // 防止过度旋转
  }
})
```

#### 4. 移动系统

**WASD移动:**
- 前进/后退: 沿视图方向
- 左/右: 垂直于视图
- 速度受地面状态影响

**跳跃机制:**
```javascript
if (onGround && keys['Space']) {
  vy = jumpForce
  onGround = false
}

// 重力
if (!onGround) {
  vy -= gravity * dt
}

// 地面碰撞
if (y <= 0) {
  y = 0
  vy = 0
  onGround = true
}
```

#### 5. 瞄准镜系统

**瞄准镜激活:**
```javascript
let scopeActive = false

canvas.addEventListener('mousedown', (e) => {
  if (e.button === 2) {  // 右键点击
    scopeActive = true
    showScopeOverlay()
  }
})
```

**瞄准镜视觉效果:**
- 径向渐晕(深色边缘)
- 十字准星
- 中心周围的刻度标记
- 中心点
- 模糊/放大效果

**视野变化:**
```javascript
if (scopeActive) {
  fov = 30  // 放大
} else {
  fov = 75  // 正常
}
```

#### 6. 射击机制

**子弹创建:**
```javascript
canvas.addEventListener('mousedown', (e) => {
  if (e.button === 0 && !reloading) {  // 左键点击
    bullets.push({
      x: player.x,
      y: player.y + eyeHeight,
      z: player.z,
      vx: Math.cos(yaw) * Math.cos(pitch) * bulletSpeed,
      vy: Math.sin(pitch) * bulletSpeed,
      vz: Math.sin(yaw) * Math.cos(pitch) * bulletSpeed
    })
  }
})
```

**射线投射命中检测:**
```javascript
for (let enemy of enemies) {
  let dist = distance(bullet, enemy)
  if (dist < hitRadius) {
    enemy.hp -= damage
    if (enemy.hp <= 0) {
      enemies.remove(enemy)
      score++
    }
  }
}
```

#### 7. 敌人AI

**生成:**
- 地图周围的随机位置
- 随时间增加数量
- 不同的敌人类型

**行为:**
- 向玩家移动
- 在射程内射击玩家
- 在障碍物后掩护

**对玩家的伤害:**
```javascript
if (distance(enemy, player) < shootRange) {
  if (random() < hitChance) {
    player.hp -= enemyDamage
    showDamageEffect()
  }
}
```

#### 8. HUD显示
```javascript
<div id="hud">
  HP: <span id="hp">100</span>
  Score: <span id="score">0</span>
</div>
```
- 生命值显示
- 分数/击杀数
- 控制说明
- 弹药计数(如果有限)

#### 9. 视觉效果

**枪口闪光:**
- 射击时的明亮闪光
- 快速淡出
- 位于枪管位置

**命中标记:**
- 命中的视觉确认
- 音效
- 敌人身上的血液飞溅

**伤害渐晕:**
- 被击中时屏幕边缘变红
- 随时间淡出
- 强度基于伤害

#### 10. 3D渲染
- 使用canvas 2D上下文和透视投影
- 使用缩放和定位模拟3D
- 深度排序以正确的绘制顺序

#### 11. 游戏循环
```javascript
function gameLoop(timestamp) {
  dt = (timestamp - lastTime) / 1000
  
  updatePlayer()
  updateEnemies()
  updateBullets()
  checkCollisions()
  
  render3DScene()
  renderHUD()
  
  requestAnimationFrame(gameLoop)
}
```

---

## DuckAdventure (鸭子冒险)

### 概述
基于网格的游戏,玩家控制鸭子收集星星,避开移动的水坑发射的弹丸。

### 核心组件

#### 1. 游戏状态
```javascript
let level = 1
let gridSize = 10 + (level - 1)  // 随等级扩展
let duckPos = 0                   // 网格索引
let score = 0
let cells = []                    // 网格单元格
let puddlePositions = []          // 敌人位置
let projectilePositions = []      // 敌人射击
```

#### 2. 网格系统
```javascript
// 网格是表示2D网格的1D数组
// 位置(row, col) = row * gridSize + col

function posToCoords(pos) {
  return {
    row: Math.floor(pos / gridSize),
    col: pos % gridSize
  }
}

function coordsToPos(row, col) {
  return row * gridSize + col
}
```

#### 3. 控制
- **方向键**: 移动鸭子(上、下、左、右)
- 移动是瞬时的(跳到相邻单元格)

#### 4. 等级进度
```javascript
function initLevel() {
  gridSize = 10 + (level - 1)  // 网格增长
  starCount = 5 + level * 2     // 更多星星
  puddleCount = 2 + level       // 更多敌人
  
  // 计时器随等级加快
  puddleMoveTimer = setInterval(movePuddles, 1000)
  puddleShootTimer = setInterval(shootProjectiles, 3000)
  projectileMoveTimer = setInterval(moveProjectiles, 300)
}
```

#### 5. 鸭子移动
```javascript
document.addEventListener("keydown", (e) => {
  const { row, col } = posToCoords(duckPos)
  
  if (e.key === "ArrowUp" && row > 0)
    duckPos -= gridSize
  else if (e.key === "ArrowDown" && row < gridSize - 1)
    duckPos += gridSize
  else if (e.key === "ArrowLeft" && col > 0)
    duckPos -= 1
  else if (e.key === "ArrowRight" && col < gridSize - 1)
    duckPos += 1
    
  checkCollisions()
})
```

#### 6. 收集品

**星星(⭐):**
- 随机放置在网格上
- 数量: 5 + level * 2
- 收集所有星星升级
- 每颗星星得分+1

**收集逻辑:**
```javascript
if (duckPos === starPos) {
  collectedStars++
  score++
  removeItem(starPos)
  
  if (collectedStars === totalStars) {
    level++
    initLevel()
  }
}
```

#### 7. 敌人(水坑)

**水坑属性:**
```javascript
{
  position: gridIndex,
  type: "puddle",
  emoji: "💧"
}
```

**水坑移动:**
```javascript
function movePuddles() {
  for (let puddle of puddlePositions) {
    // 使用寻路向鸭子移动
    let nextPos = getNextPositionTowardDuck(puddle.pos)
    if (isValidMove(nextPos)) {
      puddle.pos = nextPos
    }
  }
  checkCollisions()
}
```
- 每1000ms执行
- 向鸭子移动一个单元格
- 使用曼哈顿距离进行寻路

**水坑射击:**
```javascript
function shootProjectiles() {
  for (let puddle of puddlePositions) {
    let direction = getDirectionToDuck(puddle.pos)
    projectilePositions.push({
      pos: puddle.pos,
      direction: direction,  // 上、下、左、右
      emoji: "💦"
    })
  }
}
```
- 每3000ms执行
- 朝鸭子方向射击

#### 8. 弹丸

**弹丸移动:**
```javascript
function moveProjectiles() {
  for (let proj of projectilePositions) {
    const { row, col } = posToCoords(proj.pos)
    
    switch (proj.direction) {
      case "up":    proj.pos -= gridSize; break
      case "down":  proj.pos += gridSize; break
      case "left":  proj.pos -= 1; break
      case "right": proj.pos += 1; break
    }
    
    // 超出边界时移除
    if (isOutOfBounds(proj.pos)) {
      removeProjectile(proj)
    }
  }
  checkCollisions()
}
```
- 每300ms执行
- 向方向移动一个单元格
- 离开网格时移除

#### 9. 碰撞检测
```javascript
function checkCollisions() {
  // 鸭子 vs 水坑
  for (let puddle of puddlePositions) {
    if (duckPos === puddle.pos) {
      gameOver("Hit by puddle!")
      return
    }
  }
  
  // 鸭子 vs 弹丸
  for (let proj of projectilePositions) {
    if (duckPos === proj.pos) {
      gameOver("Hit by projectile!")
      return
    }
  }
}
```

#### 10. 视觉表示
```css
.cell            /* 绿色草地图块 */
.duck            /* 黄色背景 */
.star            /* 金色背景 */
.puddle          /* 蓝色背景 */
.projectile      /* 青色背景 */
```

每个单元格显示表情符号:
- 鸭子: 🦆
- 星星: ⭐
- 水坑: 💧
- 弹丸: 💦

#### 11. HUD显示
```javascript
<div id="level">Level: {level}</div>
<div id="score">Score: {score}</div>
```

#### 12. 寻路算法
```javascript
function getNextPositionTowardDuck(puddlePos) {
  const puddle = posToCoords(puddlePos)
  const duck = posToCoords(duckPos)
  
  // 曼哈顿距离
  const rowDiff = duck.row - puddle.row
  const colDiff = duck.col - puddle.col
  
  // 朝最大差异方向移动
  if (Math.abs(rowDiff) > Math.abs(colDiff)) {
    return puddlePos + (rowDiff > 0 ? gridSize : -gridSize)
  } else {
    return puddlePos + (colDiff > 0 ? 1 : -1)
  }
}
```

---

## Trivia (问答游戏)

### 概述
交互式问答游戏,从API获取多项选择题。

### 核心组件

#### 1. 游戏状态
```javascript
let questions = []        // 获取的问题
let currentQuestion = 0   // 索引
let score = 0            // 正确答案数
let streak = 0           // 连续正确
let playerName = ""
let selectedCategory = ""
let selectedDifficulty = ""
```

#### 2. 屏幕管理
```javascript
const screens = {
  welcome: "welcomeScreen",
  game: "gameScreen",
  results: "resultsScreen"
}

function showScreen(screenName) {
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('active')
  })
  document.getElementById(screenName).classList.add('active')
}
```

#### 3. 问题API集成
```javascript
async function fetchQuestions(category, difficulty, amount = 10) {
  const url = `https://opentdb.com/api.php?amount=${amount}&category=${category}&difficulty=${difficulty}&type=multiple`
  
  const response = await fetch(url)
  const data = await response.json()
  
  questions = data.results.map(q => ({
    question: decodeHTML(q.question),
    correct: decodeHTML(q.correct_answer),
    incorrect: q.incorrect_answers.map(decodeHTML),
    category: q.category,
    difficulty: q.difficulty
  }))
}
```

**HTML实体解码:**
```javascript
function decodeHTML(html) {
  const txt = document.createElement('textarea')
  txt.innerHTML = html
  return txt.value
}
```
- 必要,因为API返回HTML实体
- 例如: `&quot;` → `"`

#### 4. 欢迎屏幕

**姓名输入:**
```html
<input type="text" id="playerName" placeholder="Enter your name">
```

**类别选择:**
```html
<select id="category">
  <option value="9">General Knowledge</option>
  <option value="21">Sports</option>
  <option value="23">History</option>
  <!-- 等等 -->
</select>
```

**难度选择:**
```html
<select id="difficulty">
  <option value="easy">Easy</option>
  <option value="medium">Medium</option>
  <option value="hard">Hard</option>
</select>
```

#### 5. 问题显示

**打乱答案:**
```javascript
function displayQuestion() {
  const q = questions[currentQuestion]
  const allAnswers = [...q.incorrect, q.correct]
  
  // Fisher-Yates洗牌
  for (let i = allAnswers.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1))
    ;[allAnswers[i], allAnswers[j]] = [allAnswers[j], allAnswers[i]]
  }
  
  // 显示打乱的答案
  allAnswers.forEach(answer => {
    createAnswerButton(answer)
  })
}
```

**问题HTML:**
```html
<div id="questionText">What is the capital of France?</div>
<div id="questionInfo">Category: Geography | Difficulty: Easy</div>
<div id="progress">Question 1 of 10</div>
```

#### 6. 答案选择

**按钮创建:**
```javascript
function createAnswerButton(answer) {
  const button = document.createElement('button')
  button.className = 'choice'
  button.textContent = answer
  button.onclick = () => selectAnswer(answer)
  choicesContainer.appendChild(button)
}
```

**答案检查:**
```javascript
function selectAnswer(selected) {
  const correct = questions[currentQuestion].correct
  const buttons = document.querySelectorAll('.choice')
  
  // 禁用所有按钮
  buttons.forEach(btn => btn.disabled = true)
  
  // 视觉反馈
  buttons.forEach(btn => {
    if (btn.textContent === correct) {
      btn.classList.add('correct')  // 绿色
    }
    if (btn.textContent === selected && selected !== correct) {
      btn.classList.add('wrong')    // 红色
    }
  })
  
  // 更新分数
  if (selected === correct) {
    score++
    streak++
    showFeedback("Correct! ✓", "success")
  } else {
    streak = 0
    showFeedback(`Wrong! The answer was: ${correct}`, "error")
  }
  
  // 延迟后下一题
  setTimeout(nextQuestion, 2000)
}
```

#### 7. 计分系统

**分数:**
- 简单: 每个正确答案1分
- 中等: 每个正确答案2分
- 困难: 每个正确答案3分

**连续奖励:**
```javascript
if (streak >= 3) {
  bonusPoints = streak - 2  // 3连续 = +1, 4 = +2, 等等
  totalScore += bonusPoints
}
```

**视觉反馈:**
```css
.correct {
  background: #4CAF50;  /* 绿色 */
  border-color: #45a049;
}

.wrong {
  background: #f44336;  /* 红色 */
  border-color: #da190b;
}
```

#### 8. 结果屏幕

**统计显示:**
```javascript
function showResults() {
  const percentage = (score / questions.length) * 100
  
  resultsHTML = `
    <h2>Quiz Complete, ${playerName}!</h2>
    <p>Score: ${score} / ${questions.length}</p>
    <p>Percentage: ${percentage.toFixed(1)}%</p>
    <p>Best Streak: ${maxStreak}</p>
  `
  
  // 表现消息
  if (percentage === 100) {
    resultsHTML += `<p class="perfect">Perfect Score! 🏆</p>`
  } else if (percentage >= 80) {
    resultsHTML += `<p class="great">Great Job! 🎉</p>`
  } else if (percentage >= 60) {
    resultsHTML += `<p class="good">Good Effort! 👍</p>`
  } else {
    resultsHTML += `<p class="tryagain">Keep Trying! 💪</p>`
  }
}
```

**重玩选项:**
```html
<button onclick="restartQuiz()">Play Again</button>
<button onclick="changeSettings()">Change Settings</button>
<button onclick="viewLeaderboard()">Leaderboard</button>
```

#### 9. 数据持久化

**本地存储:**
```javascript
// 保存高分
function saveScore() {
  const scores = JSON.parse(localStorage.getItem('triviaScores') || '[]')
  scores.push({
    name: playerName,
    score: score,
    category: selectedCategory,
    difficulty: selectedDifficulty,
    date: new Date().toISOString()
  })
  scores.sort((a, b) => b.score - a.score)
  scores.splice(10)  // 保留前10名
  localStorage.setItem('triviaScores', JSON.stringify(scores))
}

// 加载排行榜
function loadLeaderboard() {
  return JSON.parse(localStorage.getItem('triviaScores') || '[]')
}
```

#### 10. 计时器(可选功能)
```javascript
let timeRemaining = 30  // 每题秒数

function startTimer() {
  timerInterval = setInterval(() => {
    timeRemaining--
    updateTimerDisplay()
    
    if (timeRemaining <= 0) {
      selectAnswer(null)  // 自动提交(错误答案)
    }
  }, 1000)
}

function stopTimer() {
  clearInterval(timerInterval)
}
```

#### 11. 动画
```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.choice {
  animation: fadeIn 0.3s ease-out;
}

.choice:hover:not(:disabled) {
  transform: scale(1.02);
  transition: all 0.3s;
}
```

#### 12. 错误处理
```javascript
async function fetchQuestions() {
  try {
    const response = await fetch(url)
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`)
    }
    
    const data = await response.json()
    
    if (data.response_code !== 0) {
      throw new Error('No questions available for this category/difficulty')
    }
    
    questions = data.results
  } catch (error) {
    showError('Failed to load questions. Please try again.')
    console.error('Fetch error:', error)
  }
}
```

---

## 所有游戏的通用模式

### 1. 基于Canvas的渲染
大多数游戏使用HTML5 Canvas进行渲染:
```javascript
const canvas = document.getElementById('gameCanvas')
const ctx = canvas.getContext('2d')

function draw() {
  ctx.clearRect(0, 0, canvas.width, canvas.height)
  // 绘制游戏对象
}
```

### 2. 游戏循环模式
```javascript
function gameLoop() {
  if (gameRunning) {
    update()    // 游戏逻辑
    draw()      // 渲染
    requestAnimationFrame(gameLoop)
  }
}
```

### 3. 碰撞检测
基于矩形的AABB碰撞:
```javascript
function checkCollision(a, b) {
  return a.x < b.x + b.width &&
         a.x + a.width > b.x &&
         a.y < b.y + b.height &&
         a.y + a.height > b.y
}
```

### 4. 输入处理
```javascript
const keys = {}

document.addEventListener('keydown', e => {
  keys[e.key] = true
})

document.addEventListener('keyup', e => {
  keys[e.key] = false
})

// 在更新循环中
if (keys['ArrowLeft']) player.x -= player.speed
```

### 5. 状态管理
```javascript
let gameState = 'menu'  // menu, playing, paused, gameover

function changeState(newState) {
  gameState = newState
  // 相应地更新UI
}
```

### 6. 音频系统
```javascript
const audio = new Audio('sound.mp3')
audio.volume = 0.5

function playSound() {
  audio.currentTime = 0  // 重置到开始
  audio.play()
}
```

### 7. 分数持久化
```javascript
// 保存
localStorage.setItem('highScore', JSON.stringify(score))

// 加载
const highScore = JSON.parse(localStorage.getItem('highScore') || '0')
```

---

## 技术架构

### 文件结构
每个游戏包含在单个`index.html`文件中,包含:
1. HTML结构
2. CSS样式
3. JavaScript游戏逻辑
4. 资源引用

### 性能优化
1. **对象池**: 重用子弹/敌人对象
2. **剔除**: 不渲染屏幕外对象
3. **延迟加载**: 用户交互时加载音频
4. **RequestAnimationFrame**: 流畅的60 FPS渲染

### 浏览器兼容性
- HTML5 Canvas API
- ES6+ JavaScript特性
- Web Audio API
- LocalStorage API
- Pointer Lock API(SigmaSniper)

### 资源管理
- 图像: PNG格式
- 音频: MP3格式
- 预加载: 游戏开始前加载图像
- 降级方案: 音频问题时静默失败

---

## 开发指南

### 代码风格
- 一致的缩进(2个空格)
- 描述性变量名
- 复杂逻辑的注释
- 模块化函数设计

### 测试
- 跨浏览器测试(Chrome、Firefox、Safari)
- 移动响应性(适用时)
- 性能分析
- 音频降级测试

### 未来增强
1. 多人游戏支持
2. 更多关卡/内容
3. 保存游戏进度
4. 成就系统
5. 移动触摸控制
6. 更好的图形/动画
7. 音效库
8. 分析集成

---

## 结论

这个游戏集合展示了各种游戏编程概念:
- **物理**: 重力、速度、碰撞
- **AI**: 敌人寻路、行为模式
- **用户输入**: 键盘、鼠标、触摸
- **图形**: Canvas渲染、粒子效果
- **音频**: 音效、背景音乐
- **持久化**: 本地存储、高分
- **UI/UX**: 菜单、HUD、反馈

每个游戏都作为基于浏览器的游戏开发不同方面的学习示例。
